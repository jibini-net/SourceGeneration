state {
}

interface {
}

if({System.Text.RegularExpressions.Regex.IsMatch(sp
		.GetRequiredService<Microsoft.AspNetCore.Http.IHttpContextAccessor>()
		.HttpContext
		.Request
		.Headers["User-Agent"]
		.ToString()
		.ToLowerInvariant(),
	"google|inspectiontool|bot|bing|yahoo")}

	if({Children.Count > 1}

		child({1})
	)
		
	child({0})
)
