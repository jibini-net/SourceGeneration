namespace SourceGenerator;

/*
 * Token names used as effective integer IDs in grammar.
 */
public enum Token
{
    Schema = 1,
    Partial,
    Repo,
    Service,
    Ident,
    LCurly,
    RCurly,
    LParen,
    RParen,
    Comma,
    Splat,
    Assign,
    Arrow
}
