version: '3'
services:
  website:
    build:
      context: ../
      dockerfile: TestApp/Dockerfile
    ports:
      - ${HTTP_PORT}:8080
    environment:
      - ASPNETCORE_ENVIRONMENT=${ASPNETCORE_ENVIRONMENT}
    restart: always