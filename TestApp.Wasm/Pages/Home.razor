@page "/wasm/index.html"
@using Generated

@inject ShoutBannerBase.IView shoutBanner

<c>@(shoutBanner.RenderPageAsync().Result)</c>

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" />
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.min.js"></script>

@*TODO Move assets to shared BL project*@
@*TODO Wrap components in Razor components*@
<style>
    .banner-brace {
        color: rgba(255, 255, 255, 0.6);
        font-family: monospace;
        font-size: 2rem;
        font-weight: bold;
    }

    #shout {
        width: 650px;
        max-width: 100%;
        padding: 2rem 0;
    }

        #shout div.title {
            font-size: 280%;
            font-family: monospace;
            line-height: 1;
        }

        #shout p {
            font-size: 1.5rem;
            color: #c2fbbf;
            line-height: 1.8;
        }

        #shout .btn {
            --bs-btn-color: #c2fbbf;
            --bs-btn-border-color: #c2fbbf;
            --bs-btn-hover-bg: #c2fbbf;
            --bs-btn-hover-border-color: #c2fbbf;
        }
</style>

<div style="background: purple;">
    @((MarkupString)shoutBanner.RenderPageAsync(new()).Result)
</div>
